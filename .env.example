# Canonical site URL used for metadata, sitemap, robots, and JSON-LD.
# Set this to the production domain after deploying (no trailing slash).
NEXT_PUBLIC_SITE_URL=https://your-domain.example

# Optional path prefix for project-site hosts such as GitHub Pages (no trailing slash).
# Leave empty for a custom domain or root deployment.
NEXT_PUBLIC_BASE_PATH=
